# Azure AI Services (Speech) resource endpoint — keyless (Entra ID) auth.
# The demo authenticates with DefaultAzureCredential (managed identity in Azure,
# your `az login` locally); no API keys are used.
SPEECH_ENDPOINT=https://<your-speech-resource>.cognitiveservices.azure.com
SPEECH_API_VERSION=2025-10-15
